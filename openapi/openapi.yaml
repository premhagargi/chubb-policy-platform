# Generated from the FastAPI application - do not edit by hand.
# Regenerate with: python scripts/export_openapi.py

openapi: 3.1.0
info:
  title: Chubb APAC Policy Management Platform API
  description: Policy register, portfolio analytics and AI-assisted underwriting review for Chubb's APAC
    book. All endpoints are versioned under /api/v1.
  version: 1.0.0
paths:
  /api/v1/policies:
    get:
      tags:
      - Policies
      summary: List policies
      description: Filter, search, sort and page the policy register.
      operationId: list_policies_api_v1_policies_get
      parameters:
      - name: page
        in: query
        required: false
        schema:
          type: integer
          description: 1-based page number.
          default: 1
          title: Page
        description: 1-based page number.
      - name: size
        in: query
        required: false
        schema:
          type: integer
          description: Page size (1-100).
          default: 20
          title: Size
        description: Page size (1-100).
      - name: sort
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: e.g. "premiumAmount,desc".
          title: Sort
        description: e.g. "premiumAmount,desc".
      - name: status
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Status
      - name: lineOfBusiness
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Lineofbusiness
      - name: region
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Region
      - name: effectiveDateFrom
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date
          - type: 'null'
          title: Effectivedatefrom
      - name: effectiveDateTo
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date
          - type: 'null'
          title: Effectivedateto
      - name: search
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Search
      - name: flagged
        in: query
        required: false
        schema:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Flagged
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PagedResult_PolicyDto_'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/policies/summary:
    get:
      tags:
      - Policies
      summary: Portfolio summary
      description: Aggregations over the same filtered set as GET /policies, so the KPIs always describe
        exactly what the caller is looking at.
      operationId: get_summary_api_v1_policies_summary_get
      parameters:
      - name: status
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Status
      - name: lineOfBusiness
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Lineofbusiness
      - name: region
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Region
      - name: effectiveDateFrom
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date
          - type: 'null'
          title: Effectivedatefrom
      - name: effectiveDateTo
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date
          - type: 'null'
          title: Effectivedateto
      - name: search
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Search
      - name: flagged
        in: query
        required: false
        schema:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Flagged
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PolicySummaryDto'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/policies/{policy_id}:
    get:
      tags:
      - Policies
      summary: Get a policy by id
      operationId: get_policy_api_v1_policies__policy_id__get
      parameters:
      - name: policy_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Policy Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PolicyDto'
        '404':
          description: No policy with that id.
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/policies/flag:
    patch:
      tags:
      - Policies
      summary: Flag policies for review
      description: 'Idempotent: flagging an already-flagged policy is a no-op that still reports it.'
      operationId: flag_policies_api_v1_policies_flag_patch
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FlagPoliciesRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FlagPoliciesResult'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/ai/health:
    get:
      tags:
      - AI
      summary: Which provider and model are answering
      operationId: ai_health_api_v1_ai_health_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AiHealthResponse'
  /api/v1/ai/prompt:
    post:
      tags:
      - AI
      summary: Ask the Policy Copilot a question
      description: Grounds the question in the caller's current filter - the summary aggregates plus a
        bounded sample of matching policies - then answers from that context.
      operationId: ask_api_v1_ai_prompt_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PromptRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PromptResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/ai/prompt/stream:
    post:
      tags:
      - AI
      summary: Ask the Policy Copilot, streamed token by token
      operationId: ask_streaming_api_v1_ai_prompt_stream_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PromptRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            text/event-stream: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/ai/policies/{policy_id}/risk-assessment:
    post:
      tags:
      - AI
      summary: AI underwriting risk triage for one policy
      description: Returns a structured assessment (score, band, factors, recommendation). `suggestFlag`
        is true when the model judges the policy worth flagging and it is not flagged already, which the
        UI offers as a one-click action.
      operationId: assess_policy_api_v1_ai_policies__policy_id__risk_assessment_post
      parameters:
      - name: policy_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Policy Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RiskAssessmentResponse'
        '404':
          description: No policy with that id.
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/ai/portfolio-brief:
    post:
      tags:
      - AI
      summary: Three-bullet executive brief over the current filter
      operationId: portfolio_brief_api_v1_ai_portfolio_brief_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PortfolioBriefRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PortfolioBriefResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/cache/stats:
    get:
      tags:
      - System
      summary: In-memory cache statistics
      description: 'Makes the caching layer observable rather than merely asserted - issue the

        same request twice and `hits` increments.'
      operationId: cache_stats_api_v1_cache_stats_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CacheStatsDto'
  /api/v1/cache/clear:
    post:
      tags:
      - System
      summary: Evict every cache entry
      description: Exposed so a demo can show a cold-vs-warm comparison on demand.
      operationId: clear_cache_api_v1_cache_clear_post
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CacheStatsDto'
  /api/v1/health:
    get:
      tags:
      - System
      summary: Liveness and database readiness
      operationId: health_api_v1_health_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                additionalProperties:
                  type: string
                type: object
                title: Response Health Api V1 Health Get
  /health:
    get:
      tags:
      - System
      summary: Process liveness
      description: 'Unversioned liveness probe for container orchestration.

        `/api/v1/health` additionally verifies database reachability.'
      operationId: health_health_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                additionalProperties:
                  type: string
                type: object
                title: Response Health Health Get
components:
  schemas:
    AiHealthResponse:
      properties:
        provider:
          type: string
          title: Provider
        model:
          type: string
          title: Model
        liveInference:
          type: boolean
          title: Liveinference
        detail:
          type: string
          title: Detail
      type: object
      required:
      - provider
      - model
      - liveInference
      - detail
      title: AiHealthResponse
    AiScope:
      properties:
        status:
          anyOf:
          - type: string
          - type: 'null'
          title: Status
        lineOfBusiness:
          anyOf:
          - type: string
          - type: 'null'
          title: Lineofbusiness
        region:
          anyOf:
          - type: string
          - type: 'null'
          title: Region
        effectiveDateFrom:
          anyOf:
          - type: string
            format: date
          - type: 'null'
          title: Effectivedatefrom
        effectiveDateTo:
          anyOf:
          - type: string
            format: date
          - type: 'null'
          title: Effectivedateto
        search:
          anyOf:
          - type: string
          - type: 'null'
          title: Search
        flagged:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Flagged
      type: object
      title: AiScope
      description: 'The filter the user currently has applied in the UI.


        Sent with every AI request so the model answers about the set on screen

        rather than the whole book - the difference between a demo and a feature.

        Mirrors the query parameters of GET /policies, minus paging and sort.'
    AiUsage:
      properties:
        provider:
          type: string
          title: Provider
        model:
          type: string
          title: Model
        latencyMs:
          type: integer
          title: Latencyms
        cached:
          type: boolean
          title: Cached
      type: object
      required:
      - provider
      - model
      - latencyMs
      - cached
      title: AiUsage
      description: 'Provenance for every AI response.


        Surfaced in the UI rather than logged only: a reviewer can see which

        provider and model answered, whether the answer came from the in-memory

        cache, and how long it took - which is also the evidence that the caching

        requirement is doing something.'
    CacheStatsDto:
      properties:
        hits:
          type: integer
          title: Hits
        misses:
          type: integer
          title: Misses
        hitRate:
          type: number
          title: Hitrate
        entries:
          type: integer
          title: Entries
        generation:
          type: integer
          title: Generation
        evictions:
          type: integer
          title: Evictions
      type: object
      required:
      - hits
      - misses
      - hitRate
      - entries
      - generation
      - evictions
      title: CacheStatsDto
    FlagPoliciesRequest:
      properties:
        policyIds:
          items:
            type: string
            format: uuid
          type: array
          maxItems: 500
          minItems: 1
          title: Policyids
      type: object
      required:
      - policyIds
      title: FlagPoliciesRequest
    FlagPoliciesResult:
      properties:
        flaggedPolicyIds:
          items:
            type: string
            format: uuid
          type: array
          title: Flaggedpolicyids
        flaggedCount:
          type: integer
          title: Flaggedcount
          readOnly: true
      type: object
      required:
      - flaggedPolicyIds
      - flaggedCount
      title: FlagPoliciesResult
    HTTPValidationError:
      properties:
        detail:
          items:
            $ref: '#/components/schemas/ValidationError'
          type: array
          title: Detail
      type: object
      title: HTTPValidationError
    PagedResult_PolicyDto_:
      properties:
        items:
          items:
            $ref: '#/components/schemas/PolicyDto'
          type: array
          title: Items
        page:
          type: integer
          title: Page
        size:
          type: integer
          title: Size
        totalCount:
          type: integer
          title: Totalcount
        totalPages:
          type: integer
          title: Totalpages
          readOnly: true
      type: object
      required:
      - items
      - page
      - size
      - totalCount
      - totalPages
      title: PagedResult[PolicyDto]
    PolicyDto:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        policyNumber:
          type: string
          title: Policynumber
        policyholderName:
          type: string
          title: Policyholdername
        lineOfBusiness:
          type: string
          title: Lineofbusiness
        status:
          type: string
          title: Status
        premiumAmount:
          type: number
          title: Premiumamount
        currency:
          type: string
          title: Currency
        effectiveDate:
          type: string
          format: date
          title: Effectivedate
        expiryDate:
          type: string
          format: date
          title: Expirydate
        region:
          type: string
          title: Region
        underwriter:
          type: string
          title: Underwriter
        flaggedForReview:
          type: boolean
          title: Flaggedforreview
        createdAt:
          type: string
          format: date-time
          title: Createdat
        updatedAt:
          type: string
          format: date-time
          title: Updatedat
      type: object
      required:
      - id
      - policyNumber
      - policyholderName
      - lineOfBusiness
      - status
      - premiumAmount
      - currency
      - effectiveDate
      - expiryDate
      - region
      - underwriter
      - flaggedForReview
      - createdAt
      - updatedAt
      title: PolicyDto
    PolicySummaryDto:
      properties:
        countsByStatus:
          additionalProperties:
            type: integer
          type: object
          title: Countsbystatus
        premiumByLineOfBusiness:
          additionalProperties:
            type: number
          type: object
          title: Premiumbylineofbusiness
        expiringSoonCount:
          type: integer
          title: Expiringsooncount
        flaggedCount:
          type: integer
          title: Flaggedcount
        totalCount:
          type: integer
          title: Totalcount
        countsByRegion:
          additionalProperties:
            type: integer
          type: object
          title: Countsbyregion
      type: object
      required:
      - countsByStatus
      - premiumByLineOfBusiness
      - expiringSoonCount
      - flaggedCount
      - totalCount
      - countsByRegion
      title: PolicySummaryDto
    PortfolioBriefRequest:
      properties:
        scope:
          anyOf:
          - $ref: '#/components/schemas/AiScope'
          - type: 'null'
      type: object
      title: PortfolioBriefRequest
    PortfolioBriefResponse:
      properties:
        brief:
          type: string
          title: Brief
        contextSummary:
          type: string
          title: Contextsummary
        usage:
          $ref: '#/components/schemas/AiUsage'
        createdAt:
          type: string
          format: date-time
          title: Createdat
      type: object
      required:
      - brief
      - contextSummary
      - usage
      - createdAt
      title: PortfolioBriefResponse
    PromptRequest:
      properties:
        prompt:
          type: string
          maxLength: 2000
          minLength: 1
          title: Prompt
        scope:
          anyOf:
          - $ref: '#/components/schemas/AiScope'
          - type: 'null'
      type: object
      required:
      - prompt
      title: PromptRequest
    PromptResponse:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        prompt:
          type: string
          title: Prompt
        answer:
          type: string
          title: Answer
        contextSummary:
          type: string
          title: Contextsummary
        usage:
          $ref: '#/components/schemas/AiUsage'
        createdAt:
          type: string
          format: date-time
          title: Createdat
      type: object
      required:
      - id
      - prompt
      - answer
      - contextSummary
      - usage
      - createdAt
      title: PromptResponse
    RiskAssessmentResponse:
      properties:
        policyId:
          type: string
          format: uuid
          title: Policyid
        policyNumber:
          type: string
          title: Policynumber
        riskScore:
          type: integer
          maximum: 100.0
          minimum: 0.0
          title: Riskscore
        riskBand:
          type: string
          enum:
          - Low
          - Medium
          - High
          title: Riskband
        factors:
          items:
            type: string
          type: array
          title: Factors
        recommendation:
          type: string
          title: Recommendation
        suggestFlag:
          type: boolean
          title: Suggestflag
        summary:
          type: string
          title: Summary
        usage:
          $ref: '#/components/schemas/AiUsage'
        createdAt:
          type: string
          format: date-time
          title: Createdat
      type: object
      required:
      - policyId
      - policyNumber
      - riskScore
      - riskBand
      - factors
      - recommendation
      - suggestFlag
      - summary
      - usage
      - createdAt
      title: RiskAssessmentResponse
    ValidationError:
      properties:
        loc:
          items:
            anyOf:
            - type: string
            - type: integer
          type: array
          title: Location
        msg:
          type: string
          title: Message
        type:
          type: string
          title: Error Type
        input:
          title: Input
        ctx:
          type: object
          title: Context
      type: object
      required:
      - loc
      - msg
      - type
      title: ValidationError
